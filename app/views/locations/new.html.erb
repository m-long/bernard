<% provide(:title, "Submit New Device type") %>
<% provide(:form_url, user_locations_path) %>
<% provide(:button_text, 'Add new type') %>

<h1>Submit New Location</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
