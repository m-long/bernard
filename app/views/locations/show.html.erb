<% provide(:title, full_title("#{@location.name}")) %>

<h1>
  <%= "#{@location.name}" %>
</h1>
<h2>
  Devices:
</h2>
<ul>
  <% @location.devices.each do |device| %>
    <li><%= device.name %></li>
  <% end %>
</ul>
