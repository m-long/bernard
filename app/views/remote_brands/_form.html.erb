<%= form_for(@remote_brand, url: yield(:form_url)) do |f| %>
  <%= render 'shared/error_messages', object: @remote_brand %>

  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>
