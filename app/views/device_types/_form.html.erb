<%= form_for(@device_type, url: yield(:form_url)) do |f| %>
  <%= render 'shared/error_messages', object: @device_type %>

  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>
