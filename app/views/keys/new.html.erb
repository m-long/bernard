<% provide(:title, "Submit New Key") %>
<% provide(:form_url, remote_keys_path) %>
<% provide(:button_text, 'Add new type') %>

<h1>Submit New Key</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
