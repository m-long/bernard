<% provide(:title, "Submit New Device Brand") %>
<% provide(:form_url, device_brands_path) %>
<% provide(:button_text, 'Add new brand') %>

<h1>Submit New Device Brand</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
