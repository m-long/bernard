<% provide(:title, "Submit New Device") %>
<% provide(:form_url, devices_path) %>
<% provide(:button_text, 'Add new device') %>

<h1>Submit New Device for <%= current_user.first_name %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
